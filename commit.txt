feat(embedder,benchmarks,corpus): v0.91.0 — diary-embedder entry point, full Pepys corpus, ch5 temporal flight

Promote diary_embedder to a first-class package module with a `diary-embedder` CLI
entry point; reduce benchmarks/pepys_embedder.py to a thin shim.  Rebuild the full
9-year Pepys corpus in DiaryKG (6,647 chunks, 29,402 nodes, 355,250 edges) and update
the diarykg snapshot.  Refactor pepys_mpnet_explorer.py to use raw parse_diary ingestion
(consistent format with the embedder cache), render observer flight plots, and guard
against NaN/Inf embeddings.  Extend pepys_temporal_flight.py with a fourth
temporal_backward mode, τ-reversal symmetry check, and 8-panel figure.  Add
pepys_ch5_flight.py (WaveRider Chapter 5: destination-relative temporal encoding).
Add technical disclosure doc, CodeKG analysis snapshot, and ruff format pass.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
